We need a user friendly script that is easy to launch for not computer savvy people, using mostly windows.


We need to fill the U column (PRICE_OF_ITEMS...) in the csv (e.g. `01-24.csv`) for FC_transfer which should be empty.
Each item has an ASIN (column N).
The prices are in the xlsx (amazon_asin....xlsx) where ASIN is in the column A of this xlsx.

